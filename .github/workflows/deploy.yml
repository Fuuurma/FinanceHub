name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - staging
          - production

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: finance-hub

jobs:
  deploy-staging:
    name: Deploy to Staging
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -f apps/backend/Dockerfile .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:staging
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:staging

      - name: Deploy to ECS Staging
        run: |
          aws ecs update-service --cluster finance-hub-staging \
            --service finance-hub-api \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

      - name: Run smoke tests with retry
        run: |
          # Health check with retry
          for i in {1..3}; do
            if curl -f https://staging.financehub.com/health; then
              echo "Staging health check passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "Staging health check failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

          for i in {1..3}; do
            if curl -f https://staging-api.financehub.com/api/health; then
              echo "API health check passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "API health check failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

      - name: Verify deployment
        run: |
          # Check service is healthy
          aws ecs describe-services \
            --cluster finance-hub-staging \
            --services finance-hub-api \
            --region ${{ env.AWS_REGION }} \
            --query 'services[0].deployments[0].status' \
            --output text

      - name: Notify team
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          text: |
            Staging deployment ${{ job.status }}
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}

  deploy-production:
    name: Deploy to Production
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -f apps/backend/Dockerfile .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:production
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:production
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Deploy to ECS Production
        run: |
          aws ecs update-service --cluster finance-hub-production \
            --service finance-hub-api \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

      - name: Verify deployment with retry
        run: |
          for i in {1..3}; do
            if curl -f https://financehub.com/health; then
              echo "Production health check passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "Production health check failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

          for i in {1..3}; do
            if curl -f https://api.financehub.com/api/health; then
              echo "Production API health check passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "Production API health check failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

      - name: Run smoke tests with retry
        run: |
          for i in {1..3}; do
            if curl -f https://api.financehub.com/api/v1/health; then
              echo "Smoke test 1 passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "Smoke test 1 failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

          for i in {1..3}; do
            if curl -f https://api.financehub.com/api/v1/status; then
              echo "Smoke test 2 passed (attempt $i)"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "Smoke test 2 failed after 3 attempts"
              exit 1
            fi
            echo "Retry $i/3..."
            sleep 10
          done

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          body: |
            ## Release v${{ github.run_number }}

            **Commit:** ${{ github.sha }}
            **Author:** ${{ github.actor }}

            ### Changes
            See commit history for details.

            ### Verification
            - [x] Smoke tests passed
            - [x] Health checks passed
            - [x] Deployment successful
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify team
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          text: |
            Production deployment ${{ job.status }}
            Release: v${{ github.run_number }}
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}

  rollback-staging:
    name: Rollback Staging on Failure
    if: failure()
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get previous stable task definition
        id: get-previous
        run: |
          # Get last successful deployment
          PREV_TASK=$(aws ecs describe-services \
            --cluster finance-hub-staging \
            --services finance-hub-api \
            --region ${{ env.AWS_REGION }} \
            --query 'services[0].deployments[0].taskDefinition' \
            --output text)

          echo "Previous task: $PREV_TASK"
          echo "PREV_TASK=$PREV_TASK" >> $GITHUB_OUTPUT

      - name: Rollback to previous version
        run: |
          aws ecs update-service \
            --cluster finance-hub-staging \
            --service finance-hub-api \
            --task-definition ${{ steps.get-previous.outputs.PREV_TASK }} \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

      - name: Verify rollback
        run: |
          for i in {1..5}; do
            if curl -f https://staging.financehub.com/health; then
              echo "Rollback successful (attempt $i)"
              exit 0
            fi
            echo "Waiting for rollback... ($i/5)"
            sleep 10
          done
          echo "Rollback verification failed"
          exit 1

      - name: Notify team of rollback
        uses: 8398a7/action-slack@v3
        with:
          status: 'failure'
          text: |
            🚨 ROLLBACK TRIGGERED - Staging
            Previous version restored
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}

  rollback-production:
    name: Rollback Production on Failure
    if: failure()
    needs: deploy-production
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get previous stable task definition
        id: get-previous
        run: |
          PREV_TASK=$(aws ecs describe-services \
            --cluster finance-hub-production \
            --services finance-hub-api \
            --region ${{ env.AWS_REGION }} \
            --query 'services[0].deployments[0].taskDefinition' \
            --output text)

          echo "Previous task: $PREV_TASK"
          echo "PREV_TASK=$PREV_TASK" >> $GITHUB_OUTPUT

      - name: Rollback to previous version
        run: |
          aws ecs update-service \
            --cluster finance-hub-production \
            --service finance-hub-api \
            --task-definition ${{ steps.get-previous.outputs.PREV_TASK }} \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

      - name: Verify rollback
        run: |
          for i in {1..10}; do
            if curl -f https://financehub.com/health; then
              echo "Rollback successful (attempt $i)"
              exit 0
            fi
            echo "Waiting for rollback... ($i/10)"
            sleep 10
          done
          echo "Rollback verification failed"
          exit 1

      - name: Notify team of rollback
        uses: 8398a7/action-slack@v3
        with:
          status: 'failure'
          text: |
            🚨🚨 URGENT ROLLBACK - Production 🚨🚨
            Previous version restored
            Release: v${{ github.run_number }}
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
